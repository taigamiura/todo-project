name: Todo Service CI

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master
    paths:
      - .github/workflows/todo-service-ci.yml
      - todo-service/**
      - openapi/todo-service.yaml
  pull_request:
    paths:
      - .github/workflows/todo-service-ci.yml
      - todo-service/**
      - openapi/todo-service.yaml

permissions:
  contents: read

concurrency:
  group: todo-service-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test-build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: todo-service

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: todo-service/go.mod
          cache-dependency-path: todo-service/go.sum

      - name: Test
        run: go test ./...

      - name: Build
        run: go build ./...
